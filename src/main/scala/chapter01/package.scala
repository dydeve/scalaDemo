/**
  * 一门可伸缩的语言
  * page 5
  * java线程模型依赖共享内存和锁(shared memory and locking).
  * 很难确保不发生死锁和竞态(deadlock, race condition)。
  *
  * 更安全的模式是消息传递(message passing) ——akka
  *
  * actor模型擅长并发、分布式计算
  */
package object chapter01
